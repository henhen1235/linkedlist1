#include <iostream>

using namespace std;

class Node{
 public:
  Node();
  ~Node();
  void setstudent(student* newstudent);
  char* getstudent(student* namestudent);
  void setNext(Node* newnode);
  Node* getNext();
 private:
  Student* student;
  Node* nextnode;
}
