#include <iostream>

using namespace std;

class Student{
 public:
  Student();
  ~Student();
  void makestudent(char* nfirst, char* nlast, int nID, float NGPA);
  char* getstudent(student* namestudent);
  void setNext(Node* newnode);
  Node* getNext();
 private:
  char* first;
  char* last;
  int ID;
  float GPA;
}
